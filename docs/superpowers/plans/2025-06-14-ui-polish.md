# UI Polish Implementation Plan

> **Goal:** Linear-style visual upgrade - slate palette, unified radii, bento cards, card-list tables, screenshot grids

**Architecture:** CSS-first refactoring of globals, then per-page component polish. Zero backend changes.

**Tech Stack:** Next.js 16, Tailwind CSS v4, shadcn/ui

---

### Task 1: Global CSS Reset

**Files:** `frontend/app/globals.css`

- [ ] Replace color tokens (navy→slate, adjusted muted/contrast)
- [ ] Lock radii (`--radius-sm:3px, --radius-md:4px, --radius-lg:6px`)
- [ ] Remove emoji-based scrollbar, replace with simpler version
- [ ] Add `prefers-reduced-motion` (already exists)

### Task 2: Nav Polish

**Files:** `frontend/components/layout/Nav.tsx`

- [ ] Active nav item with 2px teal underline
- [ ] Live dot: emerald + subtle pulse CSS animation
- [ ] Model selector: compact pill styling

### Task 3: Main Page Card List

**Files:** `frontend/app/page.tsx`

- [ ] Pending items: card-strip with 4px left accent border, inline edit
- [ ] Active items: progress cards with 2px thin bar, no track bg
- [ ] Section headers: counter dot, no uppercase
- [ ] Upload dropzone: reduced padding, subtle dashed border

### Task 4: History Bento Grid

**Files:** `frontend/app/history/page.tsx`

- [ ] Table → responsive card grid (`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`)
- [ ] Each card: filename, duration badge, view/delete actions
- [ ] Unviewed: left accent border + tinted bg
- [ ] Hover: `translateY(-1px)` + border glow

### Task 5: Job Detail Bento + Cards

**Files:** `frontend/app/jobs/[id]/page.tsx`, `frontend/components/results/AnalysisResults.tsx`

- [ ] Keyframe strip → bento grid (1 large + 6 small tiles)
- [ ] Each card: 3px colored accent left border
- [ ] Remove collapse (all open by default - already done)
- [ ] Score summary: mini ring/donut indicators

---
